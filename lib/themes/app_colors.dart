/*
  Define all the custom colors used across the app
 */

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color(0xFFEC8639);
  static const primaryColorLight = Color(0xFFF0A165);
  static const secondaryColor = Color(0xFFFCEEE0);
  static const inActiveBottomNavigationColor = Color(0xFF8A8A8E);
  static const activeBottomNavigationColor = Color(0xFFEC8639);
}
